# include<bits/stdc++.h>
using namespace std;
int main(){
    long long t;
    cin>>t;
    while(t--){
        long long n,k;
        cin>>n>>k;
        vector<long long>arr(n,0);
        for(long long i=0;i<n;i+=1){
            cin>>arr[i];
        }
       vector<long long>sequence;
       for(long long i=0;i<n;i+=1){
        
       }
    }
}