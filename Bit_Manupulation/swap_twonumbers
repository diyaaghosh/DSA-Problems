#include<bits/stdc++.h>
using namespace std;
void swap(int *a,int *b){
*a=(*a)^(*b);
*b=(*a)^(*b);
*a=(*a)^(*b);
}
int main(){
    int a,b;
    cout<<"enter a,b : ";
    cin>>a>>b;
    swap(a,b);
    cout<<"after swapping : ";
    cout<<a<<" "<<b;
}
